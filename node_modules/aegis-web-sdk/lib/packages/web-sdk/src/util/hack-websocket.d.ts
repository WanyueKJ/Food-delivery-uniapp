export declare type WsError = {
    readyState: number;
    msg: string;
    connectUrl: string;
};
declare type EmitError = (e: WsError) => void;
export declare type HackWsConfig = {
    key: string;
    onErr: EmitError;
    sendErr: EmitError;
};
export declare const unHackWs: (conf: HackWsConfig) => void;
declare const hackWebSocket: (config: HackWsConfig) => void;
export default hackWebSocket;
