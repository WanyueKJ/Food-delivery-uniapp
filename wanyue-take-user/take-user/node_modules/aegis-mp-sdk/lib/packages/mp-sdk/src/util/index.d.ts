import { Config } from "../../../core/src";
export declare const getCurrPageUrl: (config: Config) => string;
export declare const extend: (...args: object[]) => object;
/**
 * 对比版本号
 * 只传一个参数则默认和当前版本对比
 */
export declare const compareVersion: (ver1: string, ver2: string) => boolean;
